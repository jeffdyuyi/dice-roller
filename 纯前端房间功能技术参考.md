# 纯前端联机房间技术参考文档

本文档旨在介绍和总结本工具所采用的“纯前端、无专属后端”联机房间系统的架构方案与实现细节，方便在其他 React / Web 项目中复现类似的跑团、桌游或协作工具。

## 架构核心原理
本系统完全摒弃了传统的自建 WebSocket / HTTP 后端服务器，转而采用 **MQTT over WebSockets** 协议，依托公共（或第三方）的 MQTT Broker（例如 `wss://broker.emqx.io:8084/mqtt`）作为消息中转枢纽。
通过巧妙的 **Topic（主题）订阅与发布机制**，实现了类似于传统服务器的权限控制、点对点通信和全局广播。

---

## 1. 主持人（Host）创建房间
主持人是整个房间的枢纽。创建房间时，系统为当前会话生成一个随机的 `Room ID`。

**订阅策略 (Subscriptions):**
主持人连接到 Broker 后，会订阅以下关键 Topic：
1. **主机专属频道**：`knave/room/{roomId}/host`
   - **用途**：用于接收所有玩家发来的“加入请求”、“离开通知”以及实时的“角色卡数据同步”。
2. **全局广播频道**：`knave/room/{roomId}/broadcast`
   - **用途**：用于接收所有人的公开骰子检定、监听系统事件。
3. **大厅检索频道**：`knave_v15/lobby/requests` (可选)
   - **用途**：当其他玩家在局域网/大厅寻找房间时，主持人监听该频道的 `Ping` 并在另一个频道响应 `Pong`，从而实现房间的自动发现。

---

## 2. 玩家（Player）连接与数据同步
玩家通过手动输入 Room ID 或自动检索发现房间，并生成基于其角色 ID 的 `Player ID`。

**连接握手流程:**
1. **订阅频道**：玩家连接 Broker，订阅**全局广播频道**和**个人私密频道** (`knave/room/{roomId}/player/{playerId}`)。
2. **发送请求**：玩家向**主机专属频道**发布 `JOIN_REQUEST` 消息，附带自身的角色卡数据快照。
3. **主机响应**：主机收到请求后在界面上提示。若主机同意，则向该玩家的**个人私密频道**发布 `JOIN_RESPONSE`（包含接受状态）。
4. **状态同步**：握手成功后，玩家端只要发生任何角色数据（HP、物品、属性）变更，都会自动向主机专属频道发布 `SYNC_CHARACTER`，从而确保主持人的控制面板永远是最新的。

---

## 3. 共享聊天与公开投骰
所有的互动行为（掷骰、事件通报）都被封装成了标准的 JSON 消息格式（例如 `{ type: 'DICE_ROLL', payload: {...} }`）。

**实现方式:**
- **投骰发生**：无论是玩家还是主持人，点击投骰后，本地计算出结果（公式、最终值、各骰子值）。
- **发送广播**：将包装好的消息发布到**全局广播频道** (`knave/room/{roomId}/broadcast`)。
- **渲染视图**：所有订阅了广播频道的人（包括发送者自己），在其 `onMessage` 回调中捕捉到 `DICE_ROLL` 消息，随后更新至本地的 React 状态中（如 `EventTimeline` 组件），实现全员即时可见。

---

## 4. 暗骰机制 (Hidden Rolls)
跑团工具必备的暗骰机制在纯前端架构中通过 **“Payload 标记 + 本地过滤”** 结合的方式实现。

**实现方式:**
1. **标记**：在投掷暗骰时，消息的 Payload 中会添加标记 `isHidden: true`。
2. **发送**：消息依然被发送到**全局广播频道**。
3. **权限过滤**：接收端在处理消息时，增加一层前置判断：
   ```javascript
   if (msg.type === 'DICE_ROLL') {
     // 如果是暗骰，且当前客户端身份不是主持人 (isHost === false)，则直接丢弃该消息不予渲染
     if (msg.payload.isHidden && !isHost) return;
     
     // 否则正常添加到消息时间线
     setRoomEvents(prev => [...prev, msg]);
   }
   ```
通过这一行简单的逻辑，就实现了“只有主持人能看到暗骰结果”的安全机制。

---

## 5. 冒险备忘便签与物品发放
主持人拥有修改和干预玩家进程的权限，这涉及到了点对点的**单播通信**。

**实现方式:**
- **定义载荷 (Payload)**：便签和物品都被包装在 `SEND_MEMO` 或 `GIVE_ITEM` 消息类型中。便签内容支持纯文本或 Markdown 格式。
- **遍历分发**：
  - 如果是**全体发放**，主持人直接向**全局广播频道**发布消息。
  - 如果是**指定玩家（私密发放）**，主持人的前端代码会遍历所选玩家的列表，向每一个选中玩家的**个人私密频道** (`knave/room/{roomId}/player/{playerId}`) 逐一发送消息。
- **解析与写入**：玩家端在其个人频道收到 `SEND_MEMO` 后，由于前端集成了 Markdown 渲染引擎（如 `react-markdown`），不仅能展示优美的排版，还能将便签文本自动附加（Append）到角色卡本身的备忘录（Memo）字段中，实现数据的持久化。

---

## 总结
这套架构依靠 **MQTT Broker 作为无状态的中继器**，将本该由后端负责的房间状态管理、客户端授权全部下放到了**客户端（主要是 Host 端）的 React 状态中**管理。

**优点**：
- 零后端维护成本（可以直接白嫖各种免费公共 Broker）。
- 部署极其简单（只部署静态网页即可，完美适配 GitHub Pages / Vercel）。
- 延迟极低（MQTT 本身针对物联网设计，连接轻量，收发极速）。

**局限与改进空间**：
- 严重依赖主持人的网络（若主持人掉线，相当于“服务器宕机”，玩家数据无法同步）。目前的补偿方案是在连接断开时，利用 MQTT 的**遗嘱消息 (Will Message)** 自动通知其他端房间已关闭，防止死等。
